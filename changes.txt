Добавлено:
- класс Config.
- инициализация массивов.
- декомпиляция enum-ов.
- инициализация статических полей.
- декомпиляция примитивных классов (таких, как int.class).
- экранирование текста в строках и символах.

Исправлено:
- декомпиляция переменных, когда переменные long/double занимают два слота.
- вызов конструктора с new.
- декомпиляция if-ов, выходящих за границы родительского scope.

Теперь не выводятся синтетические поля и методы, а также автосгенерированные методы.
